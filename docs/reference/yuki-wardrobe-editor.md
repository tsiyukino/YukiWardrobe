# YukiWardrobeEditor (Editor/YukiWardrobeEditor.cs)

Custom inspector for `YukiWardrobe`. Summary only: outfit count, parameter
bits, model warnings, an error when the component is not under an avatar, and
an "Open Wardrobe Editor" button. Editing happens in the window because
Inspector-based editing loses focus whenever the Hierarchy is clicked.
